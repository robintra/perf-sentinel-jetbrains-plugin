# Findings this repository accepts, with the reasoning that earned the exception.
#
# No exceptions are currently accepted. The dependabot-cooldown exception this file used to hold
# no longer applies: Dependabot opens no version updates (.github/dependabot.yml is gone), and
# Renovate satisfies the rule's intent on its own — matured non-major updates wait seven days
# (renovate.json's minimumReleaseAge) before merging, major updates and the Rider group always go
# through a maintainer, and Gradle dependency verification pins every checksum regardless. Removed
# 2026-09-17; see DEPENDENCY-POLICY.md for the update rules.

rules: {}
